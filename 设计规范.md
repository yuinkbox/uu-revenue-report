# 营收日报 v2 设计规范

> 本文件是前端界面开发的强制约束。新增功能、优化改造，都必须与现有 v2 风格保持一致；
> 任何对视觉体系的重塑（换色、换字体、改布局范式）都需要先和用户确认，不得擅自改变。

## 1. 设计气质

- 极简、安静、专业、克制：界面没有彩色品牌色，靠**排版和数字本身**传递信息。
- 整体是"浅灰底 + 白色卡片 + 近黑文字"的中性风（zinc 色系）。
- 信息密度适中：一个页面只做一件事，区块清晰，不堆装饰。

## 2. 色彩系统（只允许使用现有 token，禁止硬编码新色值）

| Token | 用途 |
| --- | --- |
| `background` 近白灰 | 页面底色 |
| `card` 纯白 | 卡片、表格、输入框底色 |
| `foreground` 近黑 | 主文字 |
| `muted / muted-foreground` | 弱化背景、次要文字、占位说明 |
| `border` | 卡片和输入框描边 |
| `primary` 近黑 | 主按钮、选中态 |
| `destructive` 红 | 删除、对账差异、环比下降等"需要警惕" |
| 琥珀色（`amber-*`） | 警告提示条（如预览页 warnings） |
| 蓝色（`blue-*`） | 提示类信息条（如"一键导入"横幅） |

数字与文字一律用 token 色；红/蓝/琥珀只用于语义提示，不用作装饰。

## 3. 字体与数字

- 使用系统字体栈（PingFang SC / Microsoft YaHei / Segoe UI 等），基础字号 14px，行高 1.55。
- **所有金额、百分比、日期、数量等数值必须加 `.nums` 类**（等宽数字，自动对齐）。
- 金额统一用 `money()`（`¥` + 千分位 + 最多 2 位小数）；百分比用 `signedRate()` / `formatPercent()`；
  日期用现有格式化工具，不自己拼字符串。

## 4. 页面结构（新增页面的标准模板）

每个页面固定按这个顺序搭建：

1. `PageHeader`：标题 + 一句话说明 + 右侧操作按钮区（新建/导入/保存等）；
2. 关键指标：一行 `MetricCard` 网格（`grid-cols-2 lg:grid-cols-4`）；
3. 内容区：纵向排列的 `SectionCard` 区块（圆角白卡片 + 标题 + 副标题 + 图标），
   内部用 `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4` 分栏；
4. 表格用 `overflow-x-auto rounded-md border` 包裹，表头 `bg-muted/50`。

内容区整体 `mx-auto w-full max-w-[1040px]` 居中，区块间距 `flex flex-col gap-4`。

## 5. 组件使用规则

- 只使用 `src/components/ui/` 下已有的 shadcn 风格组件（Button、Input、Select、Table、
  Dialog、Alert、Textarea、RadioGroup、Badge 等），以及现成组合件：
  `PageHeader`、`SectionCard`、`MetricCard`、`Field`、`NumberInput`、`TextInput`、
  `SelectInput`、`ProductCombobox`。
- 图标只用 `lucide-react`，风格、粗细保持一致。
- **不引入新的 UI 组件库、不新增前端依赖**（除非用户明确同意）。
- 按钮尺寸统一：主操作默认 `Button`，次要 `variant="outline"`，危险操作配 `variant="ghost"` + 红色文字。

## 6. 交互与反馈

- 操作结果用 `sonner` toast：成功 / 警告 / 错误三态。
- 删除、覆盖等不可逆操作必须用 `AlertDialog` 二次确认。
- 提示性横幅用 `Alert`（蓝色=导入提示，琥珀色=预警，红色=对账差异）。
- 页面切换保持 hash 路由（`#history`、`#settings` 等），刷新后停留当前页。
- 保留 `Ctrl+S` 全局保存快捷键。
- 每个输入框有 `hint` 说明用途，需要时标 `required`。

## 7. 文案风格

- 中文、简洁、直白；按钮用动词开头（"保存设置"，不是"提交"）。
- 标签短（"总营收""台时利用率"），说明才展开解释。
- 空状态和错误要给方向（"尚未设置导出文件夹"），不指责用户。
- 同一动作在按钮、toast、标题里用同一个词（如"保存"就是"保存"）。

## 8. 新功能强制检查清单

- [ ] 新页面按 PageHeader → MetricCard → SectionCard 结构搭建；
- [ ] 新指标用 MetricCard，数值加 `.nums` 并用 `money()`/`signedRate()` 格式化；
- [ ] 颜色只用现有 token，无硬编码色值；
- [ ] 新图标取自 lucide-react，无新依赖；
- [ ] 破坏性操作有二次确认，操作结果有 toast；
- [ ] 数字口径与现有计算逻辑（`src/lib/calc.js`）保持一致；
- [ ] 移动端/窄屏不破版（响应式栅格已有，沿用即可）；
- [ ] 视觉风格的任何"升级"先征求用户同意，再动手。
