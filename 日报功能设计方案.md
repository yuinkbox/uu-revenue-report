# 营收日报 v2.5 功能设计方案

> 依据 2026-08-05 与店主确认的需求整理。开发前必须先读本文件 + `设计规范.md`。

## 1. 定位与原则

- 产品结构：**日报（日清日结）+ 周报（周度经营分析）**。
- 日报 = 商云宝数据的自动装配线 + 资金核对台 + 股东汇报出口，简洁直观；
- 周报 = 把商云宝各报表全部导入，按周汇总分析，深度复盘。
- 商云宝只管经营记录，农商银行才是资金实收依据（农商码直进卡、拉卡拉自动提现、现金留店）。
- 美团/抖音核销：一律从平台后台查看、手工录入，不做自动导入（平台后台最准）。
- 日常使用以手工录入为主（约 10 个数字），商云宝经营报表导入为可选加速，不导入也能正常出日报。
- 保持轻量：不做库存管理、会员档案、排班、平台评价、云账号体系。

## 2. 角色与权限

- **员工模式（前台）**：只显示并编辑第一段（录入区），可提交；看不到银行实收、对账差异、股东版。
- **店长模式**：输密码进入；全部可见可编辑；可核对、定稿、导出、发送。
- 界面风格、组件、文案一律遵循 `设计规范.md`。

## 3. 数据来源与分工

经营报表只有店长能从商云宝导出，因此导入和对账归店长；团购核销、现金只有前台能拿到，归前台。

| 数据 | 来源 | 方式 |
| --- | --- | --- |
| 营业额分项、客单总数、储值消费 | 商云宝「经营报表」 | 店长一键导入（自动带入） |
| 美团/抖音核销单数、金额 | 美团/抖音平台后台 | 手填（固定） |
| 会员充值金额、赠送金额、新增会员 | 前台现场知晓 | 前台手填（3 个数） |
| 当日现金收款 | 前台交班点算 | 手填 |
| 农商卡当日到账 | 农商银行网银 | 店长手填（后续支持流水导入） |
| 当日现金存入 | 前台/店长 | 手填，仅在存款当天 |

会员储值报表、会员卡消费报表**不进日报**，作为周报数据源。

## 4. 前台录入区（第一段）

区块与字段：

- 团购核销：美团单数/金额、抖音单数/金额（手填）
- 会员：充值金额、赠送金额、新增会员数（储值消费由经营报表导入自动带入，前台不填）
- 现金：当日现金收款
- 备注：异常、客诉、待办

营业额分项、客单总数由店长导入经营报表后自动带入，前台不填、不碰。

自动校验：

- 店长导入后：台桌 + 商品 + 教练 + 其他 = 总营业额，不一致当场标红；
- 客单价自动计算；
- 导入经营报表时自动带入储值消费，前台只核对。

前台点「提交日报」后第一段锁定，状态进入「待核对」。

## 5. 店长核对区（第二段）

操作顺序：

1. 店长导入商云宝「经营报表」→ 自动带入营业额分项、客单总数、储值消费；
2. 店长录入农商卡当日到账（以及当日现金存入，如有）；
3. 软件自动展示对账桥并计算差异。

自动展示对账桥，每个组成项都可见，不让店长自行判断：

```
应到账   = 营业额 + 储值充值 − 储值消费          （自动）
实际到账 = 当日现金收款 + 农商卡当日到账 − 当日现金存入   （自动）
差异     = 实际到账 − 应到账                     （自动）
```

- 农商卡当日到账：店长手填（或日后导入银行流水）；
- 当日现金存入：存款当天登记，自动从卡到账剔除，避免重复计算；
- 差异三档：容差内（绿·正常）、有原因（黄·已解释）、查不清（红·待查）；
- 容差阈值在设置中可调（建议 2~5 元），阈值内不打扰店长；
- 待查差异每日提醒，直到填写结论（原因 + 处理结果）；
- 商云宝错误记录：选填，记录哪笔有问题、如何处理；
- 店长点「定稿」锁定整份，点「发送股东」生成股东版。

## 6. 状态流转与留痕

```
录入中 → 已提交(待核对) → 核对中 → 已定稿 → 已发送
```

- 前台先填团购核销、现金、会员信息并提交；
- 店长随后导入经营报表、录入银行到账、核对差异并定稿；
- 每个状态记录操作人与时间；
- 定稿后修改需先解锁，改动留痕；
- 历史页按状态筛选，卡片显示状态徽章。

## 7. 输出

- 员工版：第一段 + 汇总（交班、内部存档）；
- 股东版：全部数据 + 对账结论（应到账、银行实收、差异及原因），仅店长可导出；
- 微信文案、PDF、Excel、长图均分两版；
- 定稿后自动上传云文档（飞书多维表格，后续接入），股东手机随时查看。

## 8. 周报功能（新增）

定位：每周一次，店长把商云宝报表按周导出、一键导入，自动按日聚合 + 周汇总，输出经营复盘。

数据来源（周维度导出）：

- 经营报表（每日一条）；
- 会员储值报表、会员卡消费报表；
- 商品综合报表、库存变动明细报表；
- 第三方平台报表（团购核销明细）。

周报内容：

- 周营收、日均营收、环比上周；
- 每日营收趋势（总营业额/台桌/商品/教练）；
- 台桌运营：周翻台率、台时利用率、高峰时段；
- 团购核销汇总：美团/抖音 单数、金额；
- 会员分析：充值金额、赠送金额、储值消费、新增会员、消费笔数；
- 商品分析：TOP 商品销量与毛利、分类占比；
- 库存与异常：出入库汇总、低库存预警、异常记录汇总；
- 周对账：周差异汇总与处理结论；
- 店长复盘结论（手写一段）。

操作流程：**每周一**导出上周一至周日（自然周）的报表 → 一键导入 → 自动生成周报 → 核对定稿 → 导出 PDF/Excel/微信文案 → 上传云文档。

输出：周报分店长版与股东版，**周报同样给股东看**；会员新增数按前台手填数周汇总。

日报在次日汇报前一日（上午 12 点前打开软件默认取昨日日期）。

## 9. 设置项新增

- 商云宝「经营报表」导出文件夹（可选导入用）；
- 对账容差阈值（默认 3 元）；
- 店长密码。

## 10. 明确不做

- 美团/抖音核销自动导入；
- 库存管理、会员档案、排班、平台评价；
- 云账号体系（仅本地角色切换）；
- 日报引入会员报表等多余导入（这些归周报）。
