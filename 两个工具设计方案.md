# 两个工具软件设计方案（V3）

> 状态：设计稿，待店主确认后开发。本方案取代《日报功能设计方案.md》中的"员工版/店长版"思路。

## 0. 背景与定位

- 放弃"一个软件 + 员工版/店长版"的方向，改为两个用途完全独立的轻量小工具，共用同一套代码基底。
- 工具一：**经营报告生成器**（店主/店长个人电脑使用）——填入数据，自动生成 日/周/季/半年/年 营收报告。
- 工具二：**前台盘货与进销存**（前台电脑使用）——每班次盘货、记录、手输，自动生成财务要的进销存表，并与商云宝对照确保库存真实可靠。
- 原则：轻量、只做辅助、不重复商云宝；界面风格严格遵守《设计规范.md》；不新增前端依赖。

## 1. 共同基底（两个工具共用）

| 项 | 方案 |
| --- | --- |
| 技术栈 | 现有 React 19 + TypeScript + Tailwind + shadcn/ui，桌面壳 pywebview + PyInstaller |
| 代码组织 | 同一个工程、两个应用入口，分别打包成两个 exe：`营收报告.exe`、`前台盘货进销存.exe` |
| 数据存储 | 各自 exe 旁的「数据」文件夹（JSON）；支持 exe 旁「配置.json」指定共享目录 |
| 设计规范 | 完全沿用现有风格：浅灰底白卡片、等宽数字、统一组件、固定页面结构 |
| 复用能力 | 商云宝报表解析（经营报表/商品综合/库存变动明细等）、金额与日期格式化、导出工具 |

## 2. 工具一：经营报告生成器（个人用）

### 2.1 使用流程

新建报告（选择周期类型 + 日期范围）→ 填入数据 → 实时预览 → 导出 / 保存。历史报告可查看、复制、删除。

### 2.2 周期类型

| 周期 | 日期范围 | 报告重点 |
| --- | --- | --- |
| 日报 | 任意一天 | 当日营收、客单、台桌、团购、会员、对账 |
| 周报 | 任意周一~周日（默认上周） | 周合计 + 日均 + 环比上周 |
| 季报 | 任意连续 3 个月（默认自然季） | 季合计 + 月拆解 + 目标达成 |
| 半年报 | 任意连续 6 个月（默认自然半年） | 半年合计 + 月趋势 + 同比 |
| 年报 | 任意连续 12 个月（默认自然年） | 全年合计 + 月度/季度拆解 + 同比 |

### 2.3 报告内容（按周期自动调整详细程度）

- 营收概况：总营收、台桌/商品/教练/其他分项、占比、客单价、环比/同比（有上期数据时自动算）、月度目标达成率；
- 台桌运营：开台数、台时利用率、高峰时段；
- 团购核销：美团/抖音 单数与金额；
- 会员：充值金额、赠送、新增会员、储值消费；
- 资金对账：应到账、实际到账（现金+银行−存现）、差异与结论（复用现有对账桥公式）；
- 商品与毛利：商品销售额、毛利、毛利率（选填）；
- 总结：店长备注/复盘。

### 2.4 数据输入

- **手填为主**：每个周期直接填汇总数（总营收、分项、客单等 10~15 个数），不依赖每日录入；
- **可选导入**：从商云宝导出经营报表/综合报表一键带入，减少手填；
- 自动计算：合计、占比、环比/同比、目标达成、对账差异全部自动。

### 2.5 输出

- 预览（报告版式，日/周报简洁、季/半年/年报更完整）；
- 导出：PDF、Word、Excel、长图 PNG；
- 微信文案（店主自己发群/给股东用）；
- 本地保存历史，可再次打开编辑。

## 3. 工具二：前台盘货与进销存

### 3.1 使用流程（前台）

维护商品档案 → 每班次新建盘货单 → 系统带入上期结余 → 录入当日入库/出库（分类型）→ 录入实盘数量 → 自动算账面与盘差 → 提交。财务需要时一键导出进销存表；店长/前台定期导入商云宝报表对照。

### 3.2 商品档案

- 字段：商品名称、分类、单位、成本单价、售价、期初库存、期初日期；
- 首次可从商云宝「商品综合报表」一键导入商品列表，之后手工增删改；
- 成本单价用于进销存金额核算（按当前成本价，后续可支持加权平均）。

### 3.3 班次盘货单

每条记录：日期、班次（早班/晚班/全天，可在设置中自定义）、操作人、明细行：

| 字段 | 说明 |
| --- | --- |
| 期初结余 | 自动带入上一班次/上一日期的期末 |
| 入库数量 | 当日进货/调拨入库 |
| 出库-销售 | 卖出数量 |
| 出库-赠送 | 赠送数量 |
| 出库-损耗/报损 | 损坏、过期等 |
| 出库-丢失 | 丢失数量 |
| 出库-其他 | 其他出库（可备注） |
| 实盘数量 | 班次结束盘点的实际数量 |

自动计算：

- 账面结余 = 期初 + 入库 − 各出库合计；
- 盘差 = 账面结余 − 实盘数量（正=盘盈，负=盘亏）；
- 盘差不为 0 时标红，提交前需填写原因（选填）。

### 3.4 进销存表（财务要的表）

按商品汇总，支持任意日期范围：

| 列 | 内容 |
| --- | --- |
| 商品/分类/单位 | 档案信息 |
| 期初数量 | 范围开始时的结余 |
| 本期入库 | 进货合计 |
| 本期出库 | 销售/赠送/损耗/丢失/其他 分列 |
| 期末账面 | 期初+入库−出库 |
| 期末实盘 | 盘点数量 |
| 盘差 | 账面−实盘 |
| 成本金额 | 期末账面 × 成本单价 |

导出为 Excel（财务格式，含汇总页 + 明细页），支持打印。

### 3.5 与商云宝对照（确保库存真实）

对照逻辑：

1. 导入商云宝「库存变动明细报表」或「商品综合报表」；
2. 软件按商品汇总商云宝的入库/出库（按变动类型分类）；
3. 与前台手工账对比：入库差异、出库差异、期末结余差异；
4. 生成「对照差异表」：差异为 0 绿、有差异红；
5. 店长处理：查明原因、填写结论，形成台账（何时、谁、差异多少、原因）。

目的：商云宝是系统账，前台是实物账；两边对得上，库存才可信。

### 3.6 数据

- 本地保存；设置页可配置共享数据文件夹（两台电脑同库）；
- 商品档案、盘货单、进销存、对照台账分开存储。

## 4. 明确不做（保持轻量）

- 不替代商云宝：不做会员档案、排班、平台评价、复杂库存核算（商云宝管系统流水）；
- 工具一不做多人协作、权限、云同步（个人用；后续可接飞书云文档）；
- 工具二 v1 不做扫码枪/电子秤/自动盘点设备对接（后续可加）；
- 不做手机端；
- 不保留旧"员工版/店长版"角色切换与密码体系。

## 5. 开发计划（确认后执行）

1. 阶段一：工程入口拆分——一个代码库、两个应用入口、两个 exe，保留公共组件/存储/导入能力；
2. 阶段二：工具一（经营报告生成器）——周期、填数、预览、导出、历史；
3. 阶段三：工具二（前台盘货与进销存）——商品档案、班次盘货、进销存表、商云宝对照；
4. 阶段四：测试、打包两个 exe、冒烟验证、更新源码包。

## 6. 待确认问题

1. 工具一的数据是**纯手填汇总数**，还是希望**支持从商云宝经营报表导入/从旧日报聚合**？
2. 工具二一天分几个班次？商品大约多少个？财务要的进销存表**有没有固定模板**（有的话发我照着做）？
3. 两个工具是否共用同一份商品档案和门店参数？
